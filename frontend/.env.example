# Base URL for the backend API.
# Leave as /api in local dev so Vite's proxy keeps everything same-origin
# (see vite.config.ts) — that's what lets the httpOnly auth cookie work.
# In deployed environments set this to the full API origin.
VITE_API_URL=/api
